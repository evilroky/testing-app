# 🧪 Testing App — система онлайн-тестирования

## 📌 Описание проекта

**Testing App** — это веб-приложение для проведения онлайн-тестирования и оценки знаний пользователей. Система позволяет проходить тесты по различным темам, автоматически подсчитывать результаты, хранить историю прохождения и управлять тестами через административный интерфейс.

Проект реализован на базе **Spring Boot** с использованием **Thymeleaf** и предназначен для демонстрации навыков backend-разработки на Java, проектирования MVC-архитектуры, работы с безопасностью, базами данных и Docker.

Проект может быть использован как:

* курсовая / дипломная работа;
* учебный проект;
* элемент портфолио Java-разработчика.

---

## 🎯 Назначение и целевая аудитория

**Назначение:**

* автоматизация процесса тестирования знаний;
* хранение и анализ результатов тестирования;
* администрирование тестов и пользователей.

**Целевая аудитория:**

* студенты и учащиеся;
* преподаватели и администраторы тестирования;
* работодатели (демонстрация архитектурных и технических навыков).

---

## ⚙️ Основной функционал

### Для пользователей:

* регистрация и авторизация в системе;
* просмотр доступных тестов;
* прохождение тестов с вопросами и вариантами ответов;
* автоматический подсчёт результата;
* просмотр результата после завершения теста;
* доступ к истории прохождения тестов;
* просмотр и редактирование профиля пользователя.

### Для администратора:

* просмотр списка пользователей;
* редактирование данных пользователей;
* создание новых тестов;
* редактирование существующих тестов;
* управление вопросами и вариантами ответов;
* просмотр отчетов и результатов тестирования.

---

## 👥 Роли пользователей

* **USER** — обычный пользователь системы, проходящий тесты и просматривающий свои результаты.
* **ADMIN** — администратор системы с доступом к управлению тестами и пользователями.

Ролевая модель реализована с использованием Spring Security.

---

## 🧱 Архитектура проекта

Проект построен по классической **многоуровневой архитектуре**:

```
Controller → Service → Repository → Database
```

### Слои:

* **Controller** — обработка HTTP-запросов, работа с UI (Thymeleaf);
* **Service** — бизнес-логика приложения;
* **Repository** — доступ к данным через Spring Data JPA;
* **Entity** — JPA-сущности базы данных;
* **Configuration** — безопасность и инициализация приложения.

---

## 🧩 Используемые технологии

### Backend:

* Java 17
* Spring Boot
* Spring MVC
* Spring Data JPA
* Spring Security
* Hibernate

### Frontend:

* Thymeleaf
* HTML5
* CSS3
* JavaScript

### База данных:

* PostgreSQL (через JPA)

### DevOps:

* Docker
* Docker Compose
* Maven

---

## 📁 Структура проекта

```
testing-app/
├── src/
│   ├── main/
│   │   ├── java/ru/egor/testingapp/
│   │   │   ├── configuration/   # Конфигурация и безопасность
│   │   │   ├── controller/       # Web-контроллеры
│   │   │   ├── entity/           # JPA-сущности
│   │   │   ├── repository/       # Репозитории данных
│   │   │   ├── service/          # Бизнес-логика
│   │   │   └── TestingAppApplication.java
│   │   └── resources/
│   │       ├── templates/        # Thymeleaf-шаблоны
│   │       ├── static/           # CSS / JS
│   │       └── application.properties
│   └── test/                     # Тесты
├── Dockerfile
├── docker-compose.yml
└── pom.xml
```

---

## 🖥️ Примеры страниц и сценариев

* **/login** — страница входа в систему;
* **/registration** — регистрация нового пользователя;
* **/tests** — список доступных тестов;
* **/test/{id}** — прохождение выбранного теста;
* **/test/result** — отображение результата;
* **/history** — история прохождения тестов;
* **/profile** — профиль пользователя;
* **/admin/** — административные страницы управления.

---

## 🚀 Инструкция по запуску

### 🔹 Запуск локально

1. Клонировать репозиторий:

```bash
git clone https://github.com/evilroky/testing-app.git
```

2. Настроить `application.properties`:

```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/testing_app
spring.datasource.username=postgres // Ваше имя пользователя
spring.datasource.password=postgres // Ваш пароль пользователя
```

3. Запустить приложение:

```bash
mvn spring-boot:run
```

Приложение будет доступно по адресу:

```
http://localhost:8081
```

---

### 🔹 Запуск через Docker

1. Убедиться, что установлен Docker и Docker Compose
2. Выполнить команду:

```bash
docker-compose up --build
```

3. Приложение будет доступно по адресу:

```
http://localhost:8081
```

4. Для остановки приложения выполните команду:

```bash
docker-compose down
```

5. Для повторного запуска приложения выполните команду:

```bash
docker-compose up
```

---

## 🔮 Возможности для расширения

* добавление таймера прохождения теста;
* поддержка нескольких попыток;
* расширенная аналитика результатов;
* экспорт отчетов в PDF/Excel;
* REST API для внешних клиентов;
* внедрение JWT-аутентификации.

---

## 👨‍💻 Автор

**Чувилов Егор**
Java / Spring Boot разработчик

Проект выполнен в учебных и демонстрационных целях.
